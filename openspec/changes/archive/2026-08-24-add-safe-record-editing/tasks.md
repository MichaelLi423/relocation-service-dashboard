# 安全记录编辑 · 实施任务

## 1. 写边界与契约

- [x] 1.1 在共享 IPC 与主进程领域服务中定义按稳定 ID 的受控编辑命令；renderer 不直接写 SQLite。
- [x] 1.2 实现仪器字段白名单与同批保存零写；跨批复用既有改批历史和运输状态校验。
- [x] 1.3 实现开单仅备注编辑、标签组/标签稳定 ID 重命名及归一化唯一性校验。

## 2. 事实更正与导入保护

- [x] 2.1 移除或拒绝 Ship-to、序列号地址、到访/工作、验收和二维码申请的原位编辑；接入新增更正或既有受保护删除重录路径。
- [x] 2.2 保持掉票撤销终态：撤销行不能编辑、重复撤销或激活，错误以新增有效掉票更正。
- [x] 2.3 保证人工编辑不写入历史导入 target snapshot/hash/来源摘要；forward-fix 发现不一致时返回冲突并不覆盖人工值。

## 3. 原子性、刷新与验证

- [x] 3.1 将状态、字段白名单、唯一性、审计与业务写入置于单一事务；拒绝或失败时零部分写入。
- [x] 3.2 成功后按 bounded 粗粒度失效标签库、项目列表、当前详情、统计与历史浏览缓存，不枚举受影响项目 ID，也不刷新导入基线。
- [x] 3.3 添加 focused 测试覆盖本 change 规格的全部场景，并运行 `npm run typecheck` 与对应 focused Vitest。
